<br>
<div class="page-header">
    <h2>Blocipedia: Social, Markdown Wikis</h2>
</div>

<div class="jumbotron">
    <p>Create and share Markdown Wikis</p>
    <p>
        <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-success' %>
    </p>
</div>


<div class="row">
    <div class="col-md-6">
        <h4>Create public wikis!</h4>
    </div>
    <div class="col-md-6">
        <h4>Create private wikis!</h4>
    </div>
</div>
<div class="row">
    <div class="col-md-6">
        <p><%= link_to "Public Wikis", new_wiki_path, class: 'btn btn-primary' %></p>
    </div>
    <div class="col-md-6">
        <% if user_signed_in? && current_user.role == 'premium' %>
            <p><%= link_to "Private Wikis", root_path, class: 'btn btn-primary' %></p>
        <% else %>
            <p><%= link_to "Premium Plan", new_charge_path, class: 'btn btn-success' %></p>
        <% end %>
    </div>
</div>
